// Standalone AI Sidebar Window Loader
import './ai_dom_sidebar.js';

// Optionally, show a message if this window can't connect to a tab
const root = document.getElementById('sidebar-root');
root.innerHTML = '';

// You can enhance this to show a warning if the window can't access the tab DOM
